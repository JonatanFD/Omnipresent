# Security

## How it works

It is really simple, we first start the server with the port that the os provide. Then a QR code is generated and displayed on the screen so that the user can scan it with their phone. This QR code contains the server's address, port and the auth_token, allowing the user to connect to the server without needing to manually enter the address.
