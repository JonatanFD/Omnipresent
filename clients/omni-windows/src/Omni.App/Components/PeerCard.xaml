<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Omni.App.Components.PeerCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Host over fingerprint with the action trailing: the same arrangement as
         the macOS "Known peers" row. The fingerprint stays on one line so rows
         keep a uniform height. -->
    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
                <TextBlock Text="{x:Bind HostLabel(Peer), Mode=OneWay}"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{x:Bind FingerprintLabel(Peer), Mode=OneWay}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           FontFamily="Consolas"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <Button Grid.Column="1" Content="{x:Bind ButtonLabel}"
                    VerticalAlignment="Center"
                    Click="OnButtonClick" />
        </Grid>
    </Border>
</UserControl>
