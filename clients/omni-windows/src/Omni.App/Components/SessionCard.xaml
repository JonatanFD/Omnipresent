<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Omni.App.Components.SessionCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- A live-session indicator, then host over role, with the action trailing:
         the same arrangement as the macOS "Active sessions" row. -->
    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Shown only while input is routed to this peer. -->
            <FontIcon Grid.Column="0"
                      Glyph="&#xE704;"
                      FontSize="16"
                      VerticalAlignment="Center"
                      Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                      Visibility="{x:Bind ActiveIndicator(Session), Mode=OneWay}" />

            <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                <TextBlock Text="{x:Bind HostLabel(Session), Mode=OneWay}"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{x:Bind RoleLabel(Session), Mode=OneWay}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <Button Grid.Column="2" Content="{x:Bind ButtonLabel}"
                    VerticalAlignment="Center"
                    Click="OnButtonClick" />
        </Grid>
    </Border>
</UserControl>
