<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Omni.App.Views.SystemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="18" MaxWidth="640">

            <!-- Section: Clipboard. Off by default and only changeable while
                 there is a daemon to tell, as on macOS. -->
            <StackPanel Spacing="8">
                <TextBlock Text="Clipboard" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="12">
                    <ToggleSwitch x:Name="ClipboardToggle"
                                  Header="Share clipboard with connected peers"
                                  IsOn="{x:Bind ViewModel.ClipboardSharing, Mode=OneWay}"
                                  IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}"
                                  Toggled="OnClipboardToggled" />
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
